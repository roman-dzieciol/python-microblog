{% extends "base.j2" %}

{% block content %}
        <h1>Hello, {{ user.username }}!</h1>
        {% for post in posts %}
        <p>{{ post.author.username }} says: {{ post.body }}</p>
        {% endfor %}
{% endblock %}